<UserControl
    x:Class="SaiBridge.Controls.GlassButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Button
        Background="Transparent"
        BorderThickness="0"
        Padding="0">

        <Border
            x:Name="GlassBorder"
            CornerRadius="18"
            BorderThickness="1"
            BorderBrush="#55FFFFFF"
            Background="#22FFFFFF">

            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="18"
                    ShadowDepth="0"
                    Opacity="0.35"/>
            </Border.Effect>

            <Grid>

                <!-- 高光 -->
                <Border
                    Height="22"
                    VerticalAlignment="Top"
                    CornerRadius="18">

                    <Border.Background>
                        <LinearGradientBrush>
                            <GradientStop
                                Color="#70FFFFFF"
                                Offset="0"/>
                            <GradientStop
                                Color="#00FFFFFF"
                                Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                </Border>

                <!-- 内容 -->
                <ContentPresenter
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"/>

            </Grid>

        </Border>

    </Button>

</UserControl>